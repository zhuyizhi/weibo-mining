实验框架：

目标：验证使用Labeled-LDA方法的有效性。

方案1：
手工构造aspect set，使用在知网情感词典中的情感词验证inference的准确性。

步骤：
a. 去除垃圾微博。用LDA分两类话题，去除其中被分到垃圾话题一类的（阈值）

b. 得到一个Labeled-LDA的训练数据集。构造时实验采用不同的隐藏话题个数（0->10）

c.上一步骤里不用的数据里，挑选含有情感词的若干条微博，进行inference，统计正确率。